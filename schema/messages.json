{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://loutrack2.local/schema/messages.json",
  "title": "Pi to Host Frame Message",
  "description": "Schema for frame messages sent from Raspberry Pi to Host via UDP/JSON",
  "type": "object",
  "required": ["camera_id", "timestamp", "blobs"],
  "properties": {
    "camera_id": {
      "type": "string",
      "description": "Unique identifier for the camera/Raspberry Pi",
      "examples": ["pi-cam-01", "pi-cam-02"]
    },
    "timestamp": {
      "type": "integer",
      "minimum": 0,
      "description": "Unix epoch timestamp in microseconds (int64). Represents the published capture timestamp.",
      "examples": [1708594800000000]
    },
    "timestamp_source": {
      "type": "string",
      "enum": ["sensor_metadata", "capture_dequeue"],
      "description": "Source used to derive the public timestamp"
    },
    "sensor_timestamp_ns": {
      "type": "integer",
      "minimum": 0,
      "description": "Raw sensor timestamp from camera metadata when available"
    },
    "host_received_at_us": {
      "type": "integer",
      "minimum": 0,
      "description": "Host-side UDP receive timestamp in Unix epoch microseconds, added by the host receiver"
    },
    "capture_to_process_ms": {
      "type": "number",
      "minimum": 0,
      "description": "Pi-side elapsed time from captured frame handoff to blob processing completion"
    },
    "capture_to_send_ms": {
      "type": "number",
      "minimum": 0,
      "description": "Pi-side elapsed time from captured frame handoff to UDP payload send"
    },
    "frame_index": {
      "type": "integer",
      "minimum": 0,
      "maximum": 4294967295,
      "description": "Monotonically increasing frame counter per camera (uint32). Used as secondary pairing key if timestamp fails.",
      "examples": [0, 1, 2]
    },
    "capture_mode": {
      "type": "string",
      "enum": ["capture", "pose_capture", "wand_metric_capture"],
      "description": "Capture mode active when this frame was emitted"
    },
    "blob_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of accepted blobs. Present for pose_capture frames."
    },
    "quality": {
      "type": "number",
      "minimum": 0,
      "description": "Single-point pose-capture quality score. Present for pose_capture frames."
    },
    "blobs": {
      "type": "array",
      "description": "Array of detected reflective marker blobs in image coordinates",
      "items": {
        "type": "object",
        "required": ["x", "y", "area"],
        "properties": {
          "x": {
            "type": "number",
            "description": "X coordinate in pixels (image coordinates, unrectified)"
          },
          "y": {
            "type": "number",
            "description": "Y coordinate in pixels (image coordinates, unrectified)"
          },
          "area": {
            "type": "number",
            "minimum": 0,
            "description": "Blob area in square pixels"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "capture_mode": { "const": "pose_capture" }
        },
        "required": ["capture_mode"]
      },
      "then": {
        "required": ["blob_count", "quality"]
      }
    }
  ],
  "additionalProperties": false
}
